# Copy this file to keystore.properties (gitignored) before ./gradlew :app:bundleRelease
# Do not commit the real passwords.

storeFile=app/play-upload.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
